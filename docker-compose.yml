version : "3.8"
services:
  webapp:
    image: faixan01/spring-webapp:v1.0
    container_name : webapp
    restart: always
    ports:
    - "9090:9090"
    networks:
    - webapp

networks:
  webapp:
